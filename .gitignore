node_modules/
.vite/
*.log
.DS_Store

# dist/ and ui/widgets/ are intentionally versioned so the EvoNexus
# installer can serve bundles without running a build step. Run
# `npm run build` and commit the result before tagging a release.
